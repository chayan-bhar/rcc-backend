services:
  - type: web
    name: rcc-backend
    runtime: docker
    branch: adding-env
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DATABASE
        sync: false
